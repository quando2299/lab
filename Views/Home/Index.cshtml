@*@{
        ViewData["Title"] = "Home Page";
    }
    @model List<ProductModel>
    <<div class="row">
        @foreach (var item in Model)
        {
            <div class="col-lg-3 col-md-6 mb-4">
                <div class="card h-100">
                    <img src="~/images/@item.ProductImage" class="img-thumbnail" width="300px" height="300px" alt="iphoneX" />
                    <div class="card-body">
                        <h4 class="cardtitle">@item.ProductName</h4>
                        <p class="cardtext">@item.ProductPrice</p>
                    </div>
                    <div class="card-footer">
                        <a asp-controller="Detail"
                           asp-action="Index" asp-route-id="@item.ProductId" asp-route-name="@item.ProductName" class="btn btn-primary">Details</a>
                        <a href="#" class="btn btn-info">Buy now</a>
                    </div>
                </div>
            </div>
        }
    </div>*@


@{
    ViewData["Title"] = "Home Page";
}
@model List<ProductModel>
<div class="row">
    @foreach (var item in Model)
    {
        <div class="col-lg-3 col-md-6 mb-4">
            <div class="card h-100">
                <img class="card-img-top"
                     src="~/images/@item.ProductImage" alt="">
                <div class="card-body">
                    <h4 class="card-title">@item.ProductName</h4>
                    <p class="card-text">@item.Descriptions</p>
                </div>
                <div class="card-footer">
                    <a asp-controller="Detail" aspaction="Index" asp-route-id="@item.ProductId" asp-route-name="@item.ProductName" class="btn btn-primary">Details</a>

                    <a asp-controller="Cart" asp-action="Buy"
                       asp-route-id="@item.ProductId" class="btn btn-info">Buy now</a>
                </div>
            </div>
        </div>
    }
</div>

